"""
Core package initialization.

Note: Metrics have moved to `middleware.metrics`. This module remains for backward compatibility,
but `core.metrics` is intentionally not re-exported to avoid duplicate definitions.
"""

